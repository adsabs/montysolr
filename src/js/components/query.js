
define(['backbone', 'underscore'], function(Backbone, _) {
  var Query = Backbone.Model;

  // Facade pattern, we want to expose only limited API
  // despite the fact that the underlying instance has
  // all power of the Backbone.Model
  return {

  }
});
