define([], function() {
  "use strict";

  // if you are not running from /, set the absolute path
  // eg root: 'http://localhost:8000/test/foo/bar'
  return {
    root: '/',
    debug: false
  }
});
