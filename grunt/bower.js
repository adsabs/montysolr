'use strict';
/**
 * Options for the `bower` grunt task
 *
 * @module grunt/bower
 */
module.exports = {
  install: {
    options: {
      cleanTargetDir: true,
      targetDir: './src/libs'
    }
  }
};
