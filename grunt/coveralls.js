'use strict';
/**
 * Options for the `coveralls` grunt task
 *
 * @module grunt/coveralls
 */
module.exports = {
  options: {
    src: ['src/js/**/*.js'],
    coverage_dir: 'test/coverage/PhantomJS 1.9.2 (Linux)/'
  }
};
