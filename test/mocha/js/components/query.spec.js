define(['libs/components/query.js'], function(require) {
  describe("API query object", function () {
      
    // Runs once before all tests start.
    before(function () {
    });
  
    // Runs once when all tests finish.
    after(function () {
    });
  
    it("should return API query object");
    
    it("accepts values with different keys");
    
    it("it can be configured to accept only given parameters");
    
    it("has single valued, and multi-valued parameters");
    
    it("can be serialized and de-serialized (saved as string and reloaded)");
    
    it("can be cloned");
    
    it("can be made immutable and throws error on modification");
    
    it("provides setters/getters but nothing else");
    
    
  });
});
